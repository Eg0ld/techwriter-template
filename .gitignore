# Dependencies
/node_modules

# Production build output
/build

# Generated files
.docusaurus
.cache-loader

# Local environment / secrets (never commit these)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# The read-only clone the init workflow makes of the watched repo
/.watched
